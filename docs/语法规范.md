# QXZ 语言规范 v1.0

QXZ 是基于 Java 开发的脚本语言，语法风格继承 Java / C++ / C# 家族（类 C 语法），并做了精简与差异化设计。

## 1. 程序结构

QXZ 程序由一系列语句组成，语句以分号 `;` 结尾，代码块使用花括号 `{ }`。

```java
let x = 1;          // 语句
func foo() { ... }  // 函数定义
```

## 2. 语言声明头（Language Header）

文件第一行可用注释形式声明程序使用的语法与版本。QXZ 内置 Java 风格语法；运行其他语法（如 Python / GO / PHP）时需要 QZ 注册对应解析器模块。

**格式（严格）：**

```
//语法名 版本号
```

**规则：**
- 语法名与版本号之间**必须有一个或多个空格**，空格是分隔符，不可省略
- 语法名不能包含空格；版本号由数字和点组成
- 不符合该严格模式的行视为普通注释（如 `//Java17` 不会被识别为声明头）

**示例：**

```
//Java 17
let x = 1;
print(x);
```

```
//Python 3
print("hello")
```

当前 QXZ v1 已注册：`Java`（版本不限）。未注册语法会在运行时明确报错。

## 3. 注释

| 语法 | 说明 |
|------|------|
| `// 注释` | 行注释 |
| `/* 注释 */` | 块注释 |
| `# 注释` | 行注释（兼容风格） |

## 4. 数据类型

| 类型 | 说明 | 示例 |
|------|------|------|
| int | 整数 | `42` |
| double | 浮点数 | `3.14` |
| string | 字符串（单双引号均可） | `"hello"` `'world'` |
| boolean | 布尔 | `true` `false` |
| null | 空值 | `null` |
| list | 列表 | `[1, 2, 3]` |
| 配置块 | section 块生成的字典 | `section app { ... }` |

## 5. 变量

使用 `let` 声明变量，类型由值推断：

```java
let x = 10;
let name = "QXZ";
let list = [1, 2, 3];
x = 20;              // 重新赋值
x += 5;              // 复合赋值（+= -= *= /=）
x++;                 // 自增
```

## 6. 运算符

| 类别 | 运算符 |
|------|--------|
| 算术 | `+` `-` `*` `/` `%` |
| 比较 | `==` `!=` `<` `<=` `>` `>=` |
| 逻辑 | `and` `or` `not` |
| 赋值 | `=` `+=` `-=` `*=` `/=` |
| 其他 | `++` `--` |

- `+` 可用于数字相加或字符串拼接
- `*` 支持字符串重复：`"abc" * 3` → `abcabcabc`
- 除法结果自动转为 double（`7 / 2` → `3.5`）

## 7. 控制流

### if / elif / else

```java
if (score >= 90) {
    print("优秀");
} elif (score >= 60) {
    print("及格");
} else {
    print("不及格");
}
```

### while

```java
let i = 0;
while (i < 10) {
    i++;
}
```

### for（类 C 三表达式）

```java
for (let i = 0; i < 10; i++) {
    print(i);
}
```

支持 `break` 跳出循环、`continue` 跳过本次迭代。

## 8. 函数

```java
func add(a, b) {
    return a + b;
}

let result = add(3, 4);   // 7
```

函数支持递归、闭包（可捕获外层变量）。

## 9. section 块（QXZ 特色语法）

`section` 是 QXZ 独有的结构化配置/数据描述语法，用于定义命名配置块，值可以是任意表达式：

```java
section server_config {
    host: "127.0.0.1";
    port: 8080;
    debug: false;
    modules: ["http", "db"];
}

print(server_config["host"]);   // 通过下标访问
```

## 10. 标准库

| 函数 | 说明 |
|------|------|
| `print(...)` | 输出，多参数用空格分隔 |
| `input()` | 读取一行输入 |
| `len(x)` | 列表长度或字符串长度 |
| `range(a, b, step)` | 生成整数列表 |
| `type(x)` | 返回值的类型名 |
| `toInt(x)` / `toDouble(x)` / `toString(x)` | 类型转换 |
| `abs(x)` / `max(a,b)` / `min(a,b)` / `sqrt(x)` / `floor(x)` / `ceil(x)` | 数学函数 |

## 11. 与 Java 的差异（设计说明）

- **无强制类型声明**：变量用 `let` 声明，类型动态推断（类似 var）
- **无类/对象体系**（v1 暂定）：以函数和数据结构为核心，简化语言模型
- **无异常体系**（v1 暂定）：错误以运行时消息形式报告
- **无 import/包管理**（v1 暂定）：单文件脚本，后续版本规划
- **新增 section 语法**：配置描述一体化
- **新增 `#` 注释**：兼容脚本语言习惯
- **逻辑运算符用英文单词**：`and` / `or` / `not`（同时词法层支持 `&&` / `||` 自动映射）
- **语言声明头**：第一行 `//语法名 版本号` 指定解析器，空格不可省略

## 12. 运行方式

QXZ 程序由 QZ 运行时执行：

```bash
java -cp build qxz.Main run program.qxz
```

运行流程：源代码 → 语言声明头识别 → 词法分析（Lexer）→ 语法分析（Parser，生成 AST）→ QZ 解释执行（Interpreter）。
